<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/content_upload_photo"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="co.tagtalk.winemate.UploadPhotoActivity"
    tools:showIn="@layout/activity_upload_photo"
    >

    <ImageView
        android:id="@+id/upload_photo_photo_to_upload"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:src="@drawable/placeholder"
        android:contentDescription="@null"/>

    <LinearLayout
        android:layout_below="@+id/upload_photo_photo_to_upload"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

    <Button
        android:id="@+id/upload_photo_select_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="@color/colorPrimaryDark"
        android:background="@android:color/transparent"
        style="?attr/buttonBarButtonStyle"
        android:text="@string/upload_photo_activity_select_button"/>

    <Button
        android:id="@+id/upload_photo_upload_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="@color/colorPrimaryDark"
        android:background="@android:color/transparent"
        style="?attr/buttonBarButtonStyle"
        android:text="@string/upload_photo_activity_upload_button"/>
    </LinearLayout>

</RelativeLayout>
